import dotenv from 'dotenv';

dotenv.config({
    path: 'environments/development.env'
});

// if you need, can export variables in here like
// export const DATABASE_PASSWORD = process.env.DATABASE_PASSWORD;
